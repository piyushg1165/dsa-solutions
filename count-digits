
int countDigits(int n){
	int digit = 0;
	int number = n;
	int count = 0;

	while(number){
		digit = number % 10;
		number = number / 10;
		if(digit != 0 && n % digit == 0)
		{
			count++;
		}
	}	
	return count;
}
